[pytest]
markers =
    integration: tests that require real running services (Chroma, retrieval-service, etc.)
    slow: tests that invoke the full multi-agent pipeline (real Bedrock calls, ~30-60s each)
